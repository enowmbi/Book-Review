<<div class="row">
  <div class="col-12">
    <h1 class="page-title text-center"><%= "Profile : #{@user.full_name.to_s.humanize}" %></h1>
    <hr/>
  </div>
</div>

<%= form_with(model: @user,local: true,class:"form-horizontal") do |f| %>
  <div class="row">
    <div class="col-sm-12 col-md-4">
      <div class="form-group">
        <%= f.label :first_name %>
        <%= f.text_field :first_name, class:"form-control", readonly: true %>
      </div>
      <div class="form-group">
        <%= f.label :last_name %>
        <%= f.text_field :last_name, class:"form-control", readonly: true %>
      </div>
    </div>
    <div class="col-sm-12 col-md-4">

      <div class="form-group">
        </div>
      <div class="text-center">
        <% if @user.image.attached? %>
          <%= image_tag(@user.medium,class: "img-responsive img-thumbnail") %>
        <% else %>
          <span class="fa fa-users fa-10x">
          <% end %>
      </div>
    </div> 
  </div>
<% end %>
<% if user_signed_in? && current_user.admin? %>
  <div class="row">
    <div class="col-12 text-center">
      <%= link_to("Edit Profile",edit_user_registration_path,class:"btn btn-outline-primary") %>
    </div>
  </div>
<% end %>
<hr/>
<div class="row">
  <div class="col-md-12 col-sm-12">
    <h5 class="text-center">Latest Books <sup><span class="badge badge-pill badge-secondary "><%= @user.books.count %> </span><sup></h5>
    <ol>
      <% @user.books.each do |book|  %>
        <li><h6><%= link_to(book.title.capitalize,book_path(book)) %></h6></li>
        <hr/>
      <% end %>
    </ol>
  </div>

  <div class="row">
    <div class="col-12 text-center">
      <%= link_to "Back", :back,class:"btn btn-outline-secondary" %>
    </div>
  </div>
